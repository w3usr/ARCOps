-r requirements.txt
pytest==9.1.1
pytest-django==4.14.0
pytest-xdist==3.8.0  # the suites are independent; -n runs them across cores
ruff==0.16.7
playwright==1.63.0   # accessibility check (TR-36); `playwright install chromium` fetches the browser
